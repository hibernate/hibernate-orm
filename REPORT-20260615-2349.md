# Gradle 10 Provider API Migration Report

## Summary

| Field | Value |
|---|---|
| Repository | `git@github.com:abstratt/hibernate-orm.git` (fork of hibernate-orm) |
| Migration status | **Migrated** — `./gradlew help` and `./gradlew assemble` both succeed |
| Local branch | `gradle-10-migration/20260615-2349-g951-to-PAPI-20260609` |
| Base branch | `main` |
| Distro pair (PAIR_ID) | `g951-to-PAPI-20260609` |
| Baseline Gradle version | `9.5.1` |
| Target distribution (TARGET_URL) | `https://services.gradle.org/distributions-snapshots/gradle-9.7.0-branch-gradle10ProviderApiMigrationRenamed-20260609103035+0000-bin.zip` |
| Build/launcher JDK | Temurin 25 (via SDKMAN) |
| Start time | `2026-06-15T23:51:15-03:00` |
| End time | `2026-06-16T00:02:19-03:00` |
| Elapsed time | `11m 4s` |
| Assistant | Claude Code / Opus 4.8 / claude-opus-4-8 |

The repository was already on the baseline Gradle version (`9.5.1`), so task 03 made no
changes. The wrapper was repointed to the Provider API preview distribution, the build scripts
and one Java source file were migrated, and both verification builds (`help`, `assemble`) passed
without any further fixes in tasks 07/08.

## Nature of changes

### Scope (from `git diff --shortstat main`)

```
6 files changed, 229 insertions(+), 13 deletions(-)
```

Per-file breakdown (`git diff --stat main`):

| File | Lines changed | Nature |
|---|---|---|
| `MIGRATION_NOTES.md` | +215 | New file — curated false-positive analysis (audit trail, not a code change) |
| `gradle/wrapper/gradle-wrapper.properties` | 4 | Distribution swap (task 04) |
| `local-build-plugins/src/main/groovy/local.java-module.gradle` | 14 | Cat-C operator-mutation rewrites |
| `tooling/metamodel-generator/hibernate-processor.gradle` | 4 | Cat-C operator-mutation rewrite |
| `local-build-plugins/.../toolchains/JavaModulePlugin.java` | 3 | Cat-B `ListProperty` getter read |
| `hibernate-core/hibernate-core.gradle` | 2 | Cat-C operator-mutation rewrite |

Excluding the `MIGRATION_NOTES.md` audit trail, the actual migration touched **5 files** with
roughly **14 insertions / 13 deletions** of real code/config.

### Build files modified

- `gradle/wrapper/gradle-wrapper.properties` — `distributionUrl` repointed to the preview
  distribution; `validateDistributionUrl` set to `false`. (No `distributionSha256Sum` was present.)
- `hibernate-core/hibernate-core.gradle`
- `local-build-plugins/src/main/groovy/local.java-module.gradle`
- `tooling/metamodel-generator/hibernate-processor.gradle`
- `local-build-plugins/src/main/java/org/hibernate/orm/toolchains/JavaModulePlugin.java`

### Transformations applied (by `migration-data.json` kind)

- **`list` (Cat-C, Groovy DSL `+=` on `ListProperty`)** — `CompileOptions.compilerArgs` and
  `BaseForkOptions.jvmArgs` (`ForkOptions`/`Test`). `prop += [ ... ]` rewritten to
  `prop.addAll( [ ... ] )` (replaces the operator mutation that no longer applies to a lazy
  `ListProperty`). Six sites across the three `.gradle` files.
- **`map` (Cat-C, Groovy DSL `+=` on `MapProperty`)** — `JavaForkOptions.systemProperties` in
  `local.java-module.gradle`. `test.systemProperties += providers.systemPropertiesPrefixedBy(...).get()`
  rewritten to the lazy `test.systemProperties.putAll( providers.systemPropertiesPrefixedBy(...) )`
  (drops the eager `.get()`, wiring the `Provider<Map>` directly).
- **`list` (Cat-B, changed-return-type getter read)** — `BaseForkOptions.getJvmArgs()` in
  `JavaModulePlugin.addJvmArgs(...)`. The getter now returns `ListProperty<String>`; the
  call `new ArrayList<>( forOptions.getJvmArgs() )` was resolved with `.get()`.

### Manual fixes beyond the migration data

None. Tasks 07 (`help`) and 08 (`assemble`) both passed on the first attempt with no edits, so
no third-party-plugin or compile-error fixes were required beyond the data-driven rewrites in
task 06.

### Known limitations / issues

- The build still reports *"Deprecated Gradle features were used in this build, making it
  incompatible with Gradle 10."* Per the migration guidelines, deprecations are intentionally
  left for now — this migration is a behavior-preserving refactor of removed/changed Provider
  API accessors, not a deprecation sweep.
- A few `test.systemProperties['key'] = value` index assignments remain in
  `local.java-module.gradle`. They are not scanner Cat-C hits (only `+=`/`-=`/`<<` operators are
  flagged) and did not break `help` or `assemble`, so they were left unchanged.

## `MIGRATION_NOTES.md` characterization

`MIGRATION_NOTES.md` exists on the branch. Both task-06 step-5(b) audits were re-run against the
file on disk:

- **Canonical-boilerplate grep (b.1):** `0` matches — no raw `apply_migrations.py` reason strings
  remain.
- **Distinct-reason check (b.2):** `entries=73 distinct=73 max_repeat=1` → **OK**.

The file is therefore a genuinely **curated** false-positive analysis: all 73 entries are
`[unconfirmed]` Category-B scanner hits, each rewritten with a site-specific reason that names the
actual receiver type at the call site (`java.io.File`, `java.lang.Class`, `org.gradle.api.Project`,
`ConfigurationContainer`/`SourceSetContainer`, worker `WorkParameters`, `java.util.Properties`,
TestKit `BuildResult`, or Hibernate domain objects such as `SchemaDescriptor`, `Inclusion`,
`SubSystem`, `SettingDescriptor`). No reason repeats more than once. None of these were real
Provider API migration sites; the scanner matched bare getter *names* (`getName`, `getVersion`,
`getPath`, `getConfigurations`, `getFile`, …) against Gradle classes the receivers do not implement.

The one CONFIRMED Cat-B hit (`BaseForkOptions.getJvmArgs()`) and all seven Cat-C operator
mutations were real; they were applied in task 06 and removed from the notes file.
</content>
