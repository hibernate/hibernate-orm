= ORM 9 Current Work Handoff

This note is a compact handoff for continuing the ORM 9 boot-model /
binding-model work in a fresh context.
It records the current branch intent, recently completed slices, deliberate
transitional bridges, useful guardrails, and the next likely migration slices.

Read this first when picking up the current branch, then follow the linked
design documents for detail.

== Current Branch Theme

The active work is moving ORM bootstrap away from direct, side-effect-heavy
`org.hibernate.mapping` construction and toward an explicit boot/mapping
pipeline:

* source discovery, XML overlay, categorization, binding, views, and
  materialization are separate phases;
* semantic binding facts are recorded before compatibility
  `org.hibernate.mapping` objects are populated;
* the contributor guide now defines a bootstrap phase contract: binding captures
  source-aware facts and resolved intents, while materialization owns
  `org.hibernate.mapping` creation/mutation;
* views over the binding model are intended to support more than one consumer,
  including mapping-object materialization and JPA static metamodel planning;
* hidden key creation and ordering side effects are being replaced by explicit
  materializers and correspondence products.

The most relevant orientation documents are:

* link:resolved-model-proposal.adoc[`resolved-model-proposal.adoc`]
* link:resolved-contribution-model-design.adoc[`resolved-contribution-model-design.adoc`]
* link:mapping-key-creation-design.adoc[`mapping-key-creation-design.adoc`]
* link:generic-inheritance-model-design.adoc[`generic-inheritance-model-design.adoc`]
* link:mapped-superclass-model-design.adoc[`mapped-superclass-model-design.adoc`]
* link:embeddable-contribution-model-design.adoc[`embeddable-contribution-model-design.adoc`]
* link:remaining-binding-gaps.adoc[`remaining-binding-gaps.adoc`]
* link:binding-extension-spi-design.adoc[`binding-extension-spi-design.adoc`]
* link:new-contributor-model-binding-guide.adoc[`new-contributor-model-binding-guide.adoc`]

== Recently Completed Direction

SessionFactory and JPA bootstrap have already moved substantially toward the
new pipeline:

* `EntityManagerFactoryBuilder` and `SessionFactoryBuilder` public staged
  bootstrap surfaces were removed.
* `SessionFactoryProducer` / construction-request based custom factory
  production is the intended custom `SessionFactory` hook.
* `SessionFactoryImpl` construction now has internal preparation products, but
  much constructor work intentionally remains in place.
* `MetadataBootstrap` / `SessionFactoryBootstrap.resolveMetadata(...)` exist as
  transitional metadata-resolution products.

Binding/model work has also moved forward:

* the internal binding model package was moved under `org.hibernate.boot.mapping`;
* binding-model declarations/usages now distinguish direct attributes,
  mapped-superclass declarations, and embeddable-site usages;
* `ValueIntent` covers singular basic, embedded, to-one, any, and plural
  collection-valued attributes;
* identifier binding has an identifier materializer path for the important
  simple, embedded, IdClass, association-id, and derived-id shapes;
* JPA static metamodel work now has view-backed planning pieces, though
  injection is still partly transitional.

== Current Uncommitted Slice

The active uncommitted slice is generic inheritance parity in the new boot
pipeline, especially generic attributes inherited from mapped superclasses and
entity superclasses.

The core problem is documented in
link:generic-inheritance-model-design.adoc[`generic-inheritance-model-design.adoc`]:
generic attributes have declaration facts, such as `T`, `List<T>`, and bounds,
and usage facts, such as `Integer`, `String`, or `List<Foo>` after inheritance
or embedding.  The boot binding model can represent this distinction with
declaration and usage bindings, but the legacy `org.hibernate.mapping`
handoff collapses it back into `Property` flags and copied `Value` objects.

Current code changes in this slice:

* `BasicValueSource` has a factory that accepts a resolved `TypeDetails` plus an
  optional explicit Java type for inherited type-variable cases.
* `BasicValueMappingMaterializer` uses that factory for inherited generic
  basic attributes so enum/converter/basic type resolution can use the concrete
  usage type while still coming from the inherited member.
* `MappedSuperTypeBinder` skips unresolved generic basic/to-one declarations
  during the initial mapped-superclass declaration pass, then creates explicit
  generic declaration views for the mapped-superclass side.
* For mapped-superclass generic basic declarations, `MappedSuperTypeBinder`
  creates a fresh `Object`-typed `BasicValue` with cloned selectables rather
  than copying an already-resolved concrete `BasicValue`.  This preserves the
  public generic JPA-domain model without re-owning DDL columns.
* `IdentifiableTypeBinder#applyGenericPropertyMarkers(...)` now detects Java
  type-variable usage from the source member signature rather than relying on
  `TypeDetails#isResolved()`.
* `EntityTypeBinder` adds concrete generic specializations for generic
  properties inherited from entity superclasses and copies the basic-value
  details needed by concrete runtime mapping.

Behavior verified in this slice:

* `GenericBasicValuedPathTest` now passes: public criteria paths for generic
  mapped-superclass id/basic attributes report `Object`, while
  `SqmPath#getResolvedModel()` resolves to `Integer` / `String`.
* The nearby generics regression set still passes:
  `SingleTableAndGenericsTest`, `EnumeratedAndGenericsTest`,
  `EmbeddableWithGenericAndMappedSuperClassTest`,
  `MappedSuperclassAndGenericsTest`, `GenericAbstractSubtypeSerializableTest`,
  and `GenericBasicValuedPathTest`.

Design/doc changes in this slice:

* Added link:generic-inheritance-model-design.adoc[`generic-inheritance-model-design.adoc`].
* Linked it from link:index.adoc[`index.adoc`].
* The design note explicitly says that generic declaration/usage facts should
  be carried toward runtime as metadata or adapter input, with
  `MetadataContext` / JPA-domain model creation as the first runtime-adjacent
  consumer.
* It also says not to make entity persisters, SQL AST, schema export, dirty
  checking, or runtime mapping types depend directly on a new generic model
  until `MetadataBuildingProcess` is gone or both boot paths populate the same
  adapter metadata.

This is still bridge work, not the final generic inheritance model.  The final
model should be part of the mapped-superclass / embeddable contribution model
once the legacy boot handoff no longer forces generic behavior through
`Property#isGeneric()`, `Property#isGenericSpecialization()`, copied values, and
`MetadataContext#addConcreteGenericAttribute(...)`.

== Deliberate Transitional Bridges

Do not treat these as accidental leftovers:

* `org.hibernate.mapping` remains the compatibility materialization output for
  now.
* Binding-model contracts should stay mapping-model-free, but materializers and
  compatibility registries may still hold `PersistentClass`, `Component`,
  `Property`, `Value`, and `Column`.
* `BindingStateImpl` still owns compatibility registries for mapping objects and
  pending work.
* Mapped-superclass and embeddable contribution/view designs are not fully
  implemented.
* `Table#createForeignKey(...)` is not deprecated yet because it is still the
  low-level table registry primitive for explicit FK creation.
* `sortProperties()`, `sortColumns(...)`, and similar ordering APIs still exist;
  the target direction is to reduce destructive sorting after explicit
  correspondence/order products cover enough paths.

== Useful Guardrails

The most recent focused guardrails for the active generic-inheritance slice
passed:

[source,bash]
----
./gradlew :hibernate-core:test \
  --tests org.hibernate.orm.test.annotations.generics.GenericBasicValuedPathTest \
  --console=plain
----

[source,bash]
----
./gradlew :hibernate-core:test \
  --tests org.hibernate.orm.test.inheritance.discriminator.SingleTableAndGenericsTest \
  --tests org.hibernate.orm.test.annotations.enumerated.EnumeratedAndGenericsTest \
  --tests org.hibernate.orm.test.annotations.embeddables.EmbeddableWithGenericAndMappedSuperClassTest \
  --tests org.hibernate.orm.test.annotations.generics.MappedSuperclassAndGenericsTest \
  --tests org.hibernate.orm.test.annotations.generics.GenericAbstractSubtypeSerializableTest \
  --tests org.hibernate.orm.test.annotations.generics.GenericBasicValuedPathTest \
  --console=plain
----

Always run:

[source,bash]
----
git diff --check
----

Expect Java deprecation warnings during Gradle runs.

== Recommended Next Slices

The best next step depends on the desired risk profile.

Recommended next slice:

* Continue the generic inheritance bucket with
  `org.hibernate.orm.test.annotations.generics.GenericsInheritanceTest`.
  Earlier focused triage showed an initialization failure with a
  `ClassCastException` in `PluralAssociationAttributeBinder`, which likely
  represents the next generic inherited plural-association shape after the
  basic/id path work.
* Keep fixes in the boot pipeline: source/categorization, binding,
  materialization, and handoff metadata.  Avoid SQL-AST/runtime behavior
  changes unless the failure proves the legacy mapping handoff cannot express
  the needed fact.
* Use link:generic-inheritance-model-design.adoc[`generic-inheritance-model-design.adoc`]
  as the local design guide.  Near-term fixes are bridges; the authoritative
  generic declaration/usage/contribution model should wait until
  `MetadataBuildingProcess` is removed or a runtime adapter is populated by
  both boot paths.
* Re-run the generics guardrail set above after each concrete fix.

Secondary next slices:

* If `GenericsInheritanceTest` turns out to be a different plural-association
  bucket, document the failure shape and decide whether to continue generic
  plural inheritance or return to the broader binding-gap list.
* If returning to model-shape work, continue mapped-superclass
  declaration/application modeling from
  link:mapped-superclass-model-design.adoc[`mapped-superclass-model-design.adoc`]
  and link:resolved-contribution-model-design.adoc[`resolved-contribution-model-design.adoc`].
* If returning to binding parity, refresh
  link:remaining-binding-gaps.adoc[`remaining-binding-gaps.adoc`] against the
  current worktree before selecting the next bucket.

If looking for correctness failures:

* refresh link:remaining-binding-gaps.adoc[`remaining-binding-gaps.adoc`] with a
  focused `hibernate-core` failure run;
* prefer bucketed fixes over broad source/categorization churn.

== Suggested Fresh-Conversation Request

Use this prompt to resume in a new conversation:

[source,text]
----
Please read design/9.0/current-work-handoff.adoc first, then switch focus back
to design/9.0/remaining-binding-gaps.adoc.  Start by refreshing the gap list
against the current worktree, taking the new bootstrap phase contract and
materialization/key-creation work into account.  Recommend the highest-signal
binding parity bucket to attack next, then begin with a focused verification
run and small, bucketed fixes.
----

== Fresh-Context Cautions

* Do not "fix" runtime entity-key or initializer behavior to compensate for
  mapping shapes that the old binding process produced correctly.  Most recent
  id/derived-id regressions were binding/materialization shape issues.
* Be careful with quoted identifiers.  Use `Identifier#matches(...)` semantics
  where identifier-aware comparison is needed; do not introduce broad
  case-insensitive string matching.
* Avoid reintroducing builder surfaces.  The design direction is explicit
  pipeline products, construction requests, producers, views, and
  materializers.
* Keep new binding-model contracts free of `org.hibernate.mapping` types unless
  the contract is explicitly a materializer or compatibility bridge.
* When adding new Java files, include `@author Steve Ebersole`.
