= ORM 9 Design Document Audit
:toc:

This document audits the design notes in `design/9.0`.

The immediate goal is to reduce stale and duplicated information.  The longer
term goal is to make each remaining document have one clear job:

* durable target design and rationale;
* contributor orientation;
* completed audit/reference data;
* historical context.

Current branch status, test status, and "what next?" guidance should live only
in link:current-work-handoff.adoc[`current-work-handoff.adoc`].  Other design
documents should not track branch health or old failure buckets.

== Classification

Active::
Still describes a live design problem or cleanup target.

Reference::
Useful durable explanation or contributor orientation, but not a status tracker.

Finished reference::
Completed reference or audit data that remains useful but is no longer an
active design note.  Keep under `finished/` when it is large enough to distract
from active routing.

Resolved::
The main design work is implemented or the audit is mostly complete.  Keep only
if it remains useful as a concise final-state reference.

Historical::
Rationale for an old PoC or transition.  Keep only if it prevents repeating an
old mistake; otherwise move to git history.

Archive candidate::
Mostly status log, stale implementation narrative, or duplicate material that
should be folded into another document before deletion.

== Policy

* `current-work-handoff.adoc` is the only current-status document.
* Design documents may say whether a design is active, resolved, or historical,
  but should not track current test health.
* Prefer one canonical document per design area.
* Prefer deleting old work logs once their final decisions are captured.
* If two documents explain the same concept, keep the one that states the
  target model most clearly and fold unique durable rationale into it.
* Completed audits should either become concise reference tables or be archived.

== Inventory

[cols="2,1,3,3", options="header"]
|===
| Document
| Classification
| Still Useful For
| Recommended Action

| link:current-work-handoff.adoc[`current-work-handoff.adoc`]
| Active
| Current stage, branch/worktree baseline, immediate exit gate, and guardrails.
| Keep as the only live status source.  Historical implementation and branch
procedures belong in git history.

| link:index.adoc[`index.adoc`]
| Active
| Entry point and document routing.
| Keep, but simplify after this audit.  It should route by canonical area and identify archive candidates.

| link:improve-bootstrap-completion-plan.adoc[`improve-bootstrap-completion-plan.adoc`]
| Active
| PR scope, stage dependencies, exit gates, and triggered follow-ups.
| Keep as the canonical completion plan.  Do not put branch status in it.

| link:bootstrap-api-compatibility.adoc[`bootstrap-api-compatibility.adoc`]
| Reference
| Removed staged builders, retained entry points, integration-hook migrations,
and compatibility decisions.
| Keep as the canonical API migration reference.

| link:boot-model-terminology.adoc[`boot-model-terminology.adoc`]
| Reference
| Stable vocabulary: declaration, usage, contribution, applied, handoff, correspondence, view.
| Keep as durable glossary.  Fold duplicate vocabulary from other docs into references to this file.

| link:new-contributor-model-binding-guide.adoc[`new-contributor-model-binding-guide.adoc`]
| Stage 5 input
| Orientation for source, XML, categorization, binding, phases, pending state, package layout.
| Replace with the three-guide structure defined by Stage 5, preserving its
durable binding guidance in the boot-model/mapping-bootstrap guide.

| link:flow-notation.adoc[`flow-notation.adoc`]
| Reference
| Shared notation for pipeline sketches.
| Keep only if still referenced by active docs.  Otherwise fold into the glossary or index.

| link:session-factory-bootstrap-design.adoc[`session-factory-bootstrap-design.adoc`]
| Resolved architecture
| Durable bootstrap target: entry points, settings, source products, mapping resolution, metadata, factory handoff.
| Keep as canonical bootstrap architecture and source material for the Stage 5
contributor guide.

| link:session-factory-construction-design.adoc[`session-factory-construction-design.adoc`]
| Resolved architecture
| Final factory construction, `SessionFactoryImpl` constructor slimming, runtime components, custom factory producers.
| Keep as the focused construction design and source material for the Stage 5
contributor guide.  Trim migration-only audit tables during consolidation.

| link:bootstrap-customizations-design.adoc[`bootstrap-customizations-design.adoc`]
| Resolved
| Ownership of mapping/type/function/session-factory customizations and `BootstrapContext` surface reduction.
| Preserve its durable ownership and ordering rules during Stage 5
consolidation; it no longer represents blocking implementation work.

| `generator-descriptor-design.adoc`
| Removed / Implemented
| Generator descriptors, runtime generator creation ownership, and schema-exportable compatibility.
| The Stage 3 implementation, contract Javadocs, completion plan, and current
handoff retain the completed decisions.  The obsolete proposal was deleted.

| `resolved-contribution-model-design.adoc`
| Removed / Implemented
| Umbrella declaration/usage/application/contribution/correspondence model
across identifiers, mapped superclasses, and embeddables.
| The binding model, materializers, handoff contracts, contract Javadocs, boot
model terminology, and contributor guide retain the completed design.  The
obsolete migration proposal was deleted.

| `declaration-usage-applied-mapping-design.adoc`
| Removed / Consolidated
| Cross-cutting declaration, contextual usage, concrete applied-mapping, compatibility-projection, and stable-role design.
| Its durable semantic levels, applied-product invariants, stable identity, and
projection-copy rules are reflected by the implemented binding model and
contributor guide; the redundant document was deleted.

| `mapped-superclass-model-design.adoc`
| Removed / Consolidated
| Long-term mapped-superclass representation and table/context ownership.
| The implemented hierarchy-specific contribution, table ownership,
role-based handoff, direct identifiable-type graph, and compatibility
projection rules remain in the binding model, handoff contracts, and
contributor guide.  The speculative mapping-class replacement proposal was
deleted.

| `applied-mapped-superclass-handoff-design.adoc`
| Removed / Implemented
| Role-based handoff from semantic mapped-superclass usage metadata to runtime consumers.
| The implemented role-based handoff, Stage 2 completion plan, contract
Javadocs, and current handoff retain the completed decisions.  The obsolete
transition note was deleted.

| `generic-inheritance-model-design.adoc`
| Removed / Consolidated
| Generic declaration/usage facts, concrete generic runtime/JPA/SQM behavior.
| Declaration/usage semantics live in the boot-model terminology, binding
contracts, and contributor guide.  Concrete static-metamodel field hiding is
covered by its implementation and tests.  The obsolete bridge proposal was
deleted.

| `embeddable-contribution-model-design.adoc`
| Removed / Consolidated
| Embeddable declaration/application products, generic embeddables, polymorphic discriminator handoff.
| Its completed contribution/application model and `Component` compatibility
boundary remain in the binding contracts, materializers, and contributor
guide.  Record-component member relationships are tracked independently by
https://github.com/hibernate/hibernate-models/issues/280[Hibernate Models issue
280].

| `identifier-correspondence-model-design.adoc`
| Removed / Rejected
| IdClass, derived identity, association-valued id correspondence and selectable ordering.
| The implemented identifier binding, extraction-kind, role handoff, target-id
conversion, and selectable correspondence already separate semantic values
from JDBC decomposition.  The speculative multi-part boot DTO was deleted; a
future unsupported shape should drive an executable runtime conversion plan.

| link:finished/deterministic-attribute-ordering-design.adoc[`finished/deterministic-attribute-ordering-design.adoc`]
| Resolved
| Phase-owned ordering and isolation of compatibility sorting.
| Finished reference.  Reopen only for a concrete ordering regression.

| `mapping-key-creation-design.adoc`
| Removed / Consolidated
| Explicit primary-key, foreign-key, unique-key, index materialization and deletion/narrowing of hidden helper hooks.
| The implemented resolved-product/materializer boundary, selectable-order
rules, table-registry ownership, and retained compatibility helpers remain in
the implementation, contract Javadocs, and contributor guide.  The slice log
was deleted.

| `metadata-building-context-audit.adoc`
| Removed / Completed
| Final mapping-layer audit of `MetadataBuildingContext` storage, transient
parameters, and completed capability splits.
| The completed conclusion remains in the Stage 2 plan and current handoff.
The historical audit log was deleted; naming-source and
`UserTypeCreationContext` SPI questions remain separate follow-ups.

| link:aggregate-mapping-design.adoc[`aggregate-mapping-design.adoc`]
| Resolved boot design / Triggered runtime follow-up
| Authoritative runtime storage descriptor, aggregate access strategy, and
consumer migration across SQL selection, assembly, JDBC access, and mutation.
| Keep as the canonical runtime aggregate design.  The boot-time mapping design
is complete; do not reopen it without a concrete unsupported mapping shape.

| `state-management-design.adoc`
| Removed / Resolved
| State-management binding model, changelog direction, runtime/settings split.
| The Stage 3 runtime audit found the existing
`StateManagement`/`AuxiliaryMapping`/`TemporalMapping`/`AuditMapping` boundary
sufficient.  The rejected descriptor proposal was deleted; future work
requires a concrete feature trigger.

| `jpa-metamodel-view-materialization-design.adoc`
| Removed / Consolidated
| Static metamodel injection plan derived from binding/materialization views.
| The implemented materialization-time plan, archive-safe handoff, field
roles, generic field hiding, and runtime injection boundary remain in the
implementation, handoff contracts, and tests.  The completed proposal was
deleted.

| link:finished/annotation-coverage.adoc[`finished/annotation-coverage.adoc`]
| Finished reference
| Annotation coverage matrix and remaining annotation-level gaps.
| Keep in the finished bucket as a completed audit.  Remaining deferred entries should point to active design areas rather than become current failure buckets.

| link:binding-extension-spi-design.adoc[`binding-extension-spi-design.adoc`]
| Active / Stabilization
| ORM 9 semantic-to-materialized contract for the incubating
`AttributeBinder` and `TypeBinder` SPIs.
| Define the supported read-only categorized and applied-model SPI, retain
`org.hibernate.mapping` as the mutable target, and stabilize binder timing.
The abandoned capability/facet facade has been removed; useful contribution
records and materializers remain internal implementation architecture.

| link:additional-mapping-contributor-design.adoc[`additional-mapping-contributor-design.adoc`]
| Resolved / Triggered Follow-up
| Implemented immutable `ProcessedMappings` view for the retained late hook,
plus the design and triggers for a possible early source-level hook.
| Keep the implemented late boundary.  Do not add the early hook until a
documented recategorization trigger occurs.

| link:metadata-serialization-design.adoc[`metadata-serialization-design.adoc`]
| Resolved
| Factory-ready archive production/restoration, payload boundaries, stable
identity, invalidation, and runtime handoff.
| Keep as the canonical metadata archive design.

| `boot-model-snapshot-design.adoc`
| Removed / Superseded
| Early alternatives and rationale which led to the implemented archive.
| Stable-identity and validation decisions are retained by
`metadata-serialization-design.adoc`; the obsolete proposal was deleted.
|===

== Consolidation Clusters

=== Bootstrap Cluster

Canonical target:

* link:session-factory-bootstrap-design.adoc[`session-factory-bootstrap-design.adoc`]
* link:session-factory-construction-design.adoc[`session-factory-construction-design.adoc`]
* link:bootstrap-customizations-design.adoc[`bootstrap-customizations-design.adoc`]
* link:metadata-serialization-design.adoc[`metadata-serialization-design.adoc`]
* link:bootstrap-api-compatibility.adoc[`bootstrap-api-compatibility.adoc`]

Current-status material from this cluster should move to
link:current-work-handoff.adoc[`current-work-handoff.adoc`] or be deleted.

=== Contribution / Handoff Cluster

Canonical target:

* link:boot-model-terminology.adoc[`boot-model-terminology.adoc`]
* link:new-contributor-model-binding-guide.adoc[`new-contributor-model-binding-guide.adoc`]

=== Mapping Integration Cluster

Canonical target:

* link:new-contributor-model-binding-guide.adoc[`new-contributor-model-binding-guide.adoc`]

Possible focused references:

* link:aggregate-mapping-design.adoc[`aggregate-mapping-design.adoc`]

The old `orm-integration-notes.adoc` scratchpad has been deleted.  Its durable
key-creation rules are reflected by the implemented materializers; its
source-descriptor and phase-ordering guidance is represented by
link:new-contributor-model-binding-guide.adoc[`new-contributor-model-binding-guide.adoc`].

=== Extension And Follow-Up Cluster

Resolved:

* link:additional-mapping-contributor-design.adoc[
  `additional-mapping-contributor-design.adoc`] for the late immutable
  processed-model view.

Active stabilization:

* link:binding-extension-spi-design.adoc[
  `binding-extension-spi-design.adoc`] for the supported semantic-to-
  materialized `AttributeBinder` and `TypeBinder` boundary.

Triggered follow-up:

* the early source-level contributor portion of
  `additional-mapping-contributor-design.adoc`.

The superseded boot-model snapshot proposal was deleted after its durable
stable-identity and validation decisions were retained by
link:metadata-serialization-design.adoc[`metadata-serialization-design.adoc`].

None should carry current branch status.

=== Finished Reference Bucket

Finished reference/audit artifacts:

* link:finished/annotation-coverage.adoc[`finished/annotation-coverage.adoc`]
* link:finished/deterministic-attribute-ordering-design.adoc[
  `finished/deterministic-attribute-ordering-design.adoc`]

== Open Question Routing

Focused design notes may retain alternatives and open questions, but their
execution scope is assigned here.  This table is authoritative when an older
question section does not yet carry an inline disposition.

[cols="2,2,4", options="header"]
|===
| Area
| Scope
| Questions To Resolve

| Bootstrap customizations
| Resolved
| Product ownership and deterministic ordering are explicit pipeline
responsibilities; broad context transport of resolved products has been
removed.

| Additional mapping contribution
| Resolved / Triggered follow-up
| The retained late contributor uses its narrow processed-model and context
views.  Early source contribution remains triggered by a concrete pre-binding
use case.

| Binding extensions
| Stabilization
| Retain `AttributeBinder` and `TypeBinder` as incubating ORM 9 SPIs.  Close the
supported read-only semantic-model surface used by their contexts and document
the materialization and resolution timing.  Treat early categorization
mutation as a distinct triggered extension problem.

| JPA static metamodel
| Resolved / Follow-up cleanup
| Implemented materialization-time plan production, embeddable and inherited
generic roles, processor/runtime field hiding, and resolved-pipeline retirement
of duplicate runtime traversal.  Remaining legacy entry-point cleanup is
follow-up.

| Aggregate mappings
| Resolved boot design / Triggered runtime follow-up
| Boot-time intent, member-container, value-plan, and finalization work is
complete.  Remaining work is an authoritative runtime storage descriptor and
access strategy, followed by staged migration of SQL selection, assembly, JDBC,
and mutation consumers.

| Generators
| Resolved / Implemented
| The common declarative `GeneratorDescriptor` covers identifier and value
generation, archive restoration, gated CDI access, schema export, and deferred
runtime creation.  Generator classes now declare exportable capability
directly.

| State management
| Resolved / Triggered follow-up
| Runtime/settings handoff verified through `StateManagement`,
`AuxiliaryMapping`, `TemporalMapping`, and `AuditMapping`, including focused
new-pipeline mutation execution.  Add a new contribution/resolution product
only if a concrete whole-model source requirement cannot use the existing
projection.

| Declaration / usage / applied mapping
| Resolved foundation
| No umbrella redesign.  Route remaining behavior to the mapped-superclass,
embeddable, generic, identifier, or runtime consumer which exposes it.

| Mapped superclasses, embeddables, and generics
| Resolved
| Stable role-based application and runtime/JPA handoffs cover mixed
components and generic specialization.  Superseded focused notes have been
consolidated or deleted.

| Identifier correspondence
| Resolved
| Do not add a parallel multi-part boot DTO.  If a concrete runtime identifier
shape cannot use semantic target-id conversion plus selectable decomposition,
introduce an executable runtime conversion plan with that consumer.

| Deterministic ordering
| Resolved
| Preserve phase-owned sorting and focused guardrails; no remaining design
question blocks the PR.

| Mapping-key creation
| Resolved
| Explicit key products and materializers are the final design.  Reopen only
for a concrete compatibility failure.

| `MetadataBuildingContext`
| Resolved / follow-up only
| Mapping objects no longer retain or expose broad context.  Total reference
count and compatibility constructor parameters are not completion metrics.
Treat naming-source and `UserTypeCreationContext` SPI narrowing separately.

| SessionFactory bootstrap/construction
| Resolved
| Explicit prepared products, narrowed runtime-model context,
service/integrator phases, final construction boundary, and retained
`SessionFactoryOptions` compatibility view are implemented.

| Metadata snapshot/serialization
| Resolved / Triggered follow-up
| The factory-ready archive is complete.  Other general-purpose snapshot layers
are follow-ups triggered by a concrete tooling or startup requirement.

| Bootstrap API compatibility
| Stabilization
| Formal `Configuration` deprecation policy, replacement coverage for removed
builder contributors, and the minimum public custom-producer settings view.
|===

== Resolved Or Mostly Resolved Items

These should not remain as active status narratives:

* removal of `MetadataSources`, `MetadataBuilder`, `MetadataBuilderImpl`,
  `HibernateBootstrap`, `SessionFactoryBuilder`, and
  `Metadata#buildSessionFactory()`;
* replacement of the old public staged builder path with
  `HibernatePersistenceConfiguration` / JPA bootstrap and internal
  `BootstrapPipeline`;
* externalization of `BasicValue.Resolution` determination;
* removal of the old collector-driven basic-value resolver registration path;
* materializer bridge drain for key creation, except for retained deprecated
  compatibility helpers;
* removal of `MetadataBuildingProcess` as a production path;
* old broad failure-bucket and green-suite closeout narratives.
