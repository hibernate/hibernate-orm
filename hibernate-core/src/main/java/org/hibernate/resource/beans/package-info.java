/**
 * Defines Hibernate's managed bean support, including integration with CDI and
 * other bean containers, and fallback instance creation. The central
 * contracts from a client point of view are
 * {@link org.hibernate.resource.beans.spi.ManagedBean} and
 * {@link org.hibernate.resource.beans.spi.ManagedBeanRegistry}.
 */
package org.hibernate.resource.beans;
