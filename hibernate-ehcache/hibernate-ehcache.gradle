apply plugin: 'java'

dependencies {
    compile( project( ':hibernate-core' ) )
    compile( [group: 'net.sf.ehcache', name: 'ehcache-core', version: '2.4.2', ext: 'jar'] )
    testCompile( project(':hibernate-testing') )
    testCompile( [group: 'org.apache.derby', name: 'derby', version: '10.5.3.0_1', ext: 'jar'] )
}

test {
//  ignoreFailures = true
}