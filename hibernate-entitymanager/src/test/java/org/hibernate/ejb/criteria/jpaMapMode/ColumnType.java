package org.hibernate.ejb.criteria.jpaMapMode;

/**
 * Ways that the storage system represents logical types. A mid-tier mapping between Hibernate and the metamodel.
 * Not sure if this is really needed.
 */
public enum ColumnType {
    TEXT, NUMERIC, ID;
}
