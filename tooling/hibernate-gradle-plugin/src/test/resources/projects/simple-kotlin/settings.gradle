/*
 * SPDX-License-Identifier: Apache-2.0
 * Copyright Red Hat Inc. and Hibernate Authors
 */

def tomlPath = System.getProperty("gradle.libs.versions.toml.path")
dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("$tomlPath"))
        }
    }
}
